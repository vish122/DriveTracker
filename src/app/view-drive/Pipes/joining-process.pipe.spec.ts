import { JoiningProcessPipe } from './joining-process.pipe';

describe('JoiningProcessPipe', () => {
  it('create an instance', () => {
    const pipe = new JoiningProcessPipe();
    expect(pipe).toBeTruthy();
  });
});
