import { DesignationPipe } from './designation.pipe';

describe('DesignationPipe', () => {
  it('create an instance', () => {
    const pipe = new DesignationPipe();
    expect(pipe).toBeTruthy();
  });
});
