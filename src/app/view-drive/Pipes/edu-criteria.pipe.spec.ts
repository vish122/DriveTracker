import { EduCriteriaPipe } from './edu-criteria.pipe';

describe('EduCriteriaPipe', () => {
  it('create an instance', () => {
    const pipe = new EduCriteriaPipe();
    expect(pipe).toBeTruthy();
  });
});
